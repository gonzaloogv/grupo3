package com.grupo3.freno.platform

import android.service.notification.NotificationListenerService

/**
 * Punto de entrada declarado para que el usuario pueda conceder acceso a notificaciones.
 * La captura y clasificación se implementan en A-01, fuera de esta feature visual inicial.
 */
class FrenoNotificationListener : NotificationListenerService()
